services:
  - type: web
    name: school-app-backend
    env: node
    plan: free
    buildCommand: "cd backend && npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
